import React from 'react';
import Project from './Project';

const ProjectList = ({ projects }) => {
    return (
        <ul className="list-projects">
            {/* {projects.map(project => (
                <Project prokectInfo={project}/>
            ))} */}
            all projects here
        </ul>
    );
}
 
export default ProjectList;